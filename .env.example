# CoreBackend (preferred) — same contract as RoomsController GET/PUT /rooms/{id}
# Dev: Vite proxies /api → http://localhost:8081 (see vite.config.ts)
VITE_API_URL=/api

# Optional fallback — direct Supabase (realtime). Used only when VITE_API_URL is unset.
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key
